<menu
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/activity_main_library"
        android:title="@string/activity_main_menu_library"
        android:icon="@drawable/ic_menu_tab_library"
        app:showAsAction="always"/>

    <item
        android:id="@+id/activity_main_playlists"
        android:title="@string/activity_main_menu_playlists"
        android:icon="@drawable/ic_menu_tab_playlist"
        app:showAsAction="ifRoom"/>
</menu>